#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../project-04-cli-based-calculator-node-and-typescript/lib/index.js" "$@"
else 
  exec node  "$basedir/../project-04-cli-based-calculator-node-and-typescript/lib/index.js" "$@"
fi
